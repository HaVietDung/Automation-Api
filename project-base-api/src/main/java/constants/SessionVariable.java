package constants;

public class SessionVariable {

    public enum Common {
        COUNTRY, SCENARIO, PATH_FEATURE, DIRECTORY_FEATURE
    }

    public enum Customer {
        TOKEN, ACCESS_TOKEN, TYPE_ACCOUNT, EMAIL, PASSWORD, GROUP_CUSTOMER, PHONE_NUMBER
    }
}
